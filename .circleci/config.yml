version: 2.1
executors:
  executor:
    docker:
      - image: tushar1998/ultimate-pnpm:lts

jobs:
  build-workspace:
    executor: executor
    steps:
      - checkout
      - run: |
          pnpm install
      - run: |
          pnpm build:packages
      - run: |
          pnpm build:apps

  version-publish:
    executor: executor
    steps:
      - checkout
      - run: |
          pnpm version:publish

workflows:
  build-packages-apps:
    jobs:
      - build-workspace
  version-publish-registry:
    jobs:
      - version-publish
